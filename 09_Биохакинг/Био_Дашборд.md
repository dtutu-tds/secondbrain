# 🧬 Bio Dashboard

## ✅ Вещества / практики, которые подходят

```dataview
TABLE WITHOUT ID
  file.link as "Название",
  effect_score as "Эффект",
  main_effects as "Основные эффекты",
  side_effects as "Побочные эффекты"
FROM "09_Биохакинг/Вещества"
WHERE status = "✅ Использовать"
SORT effect_score DESC
```

## ⚠️ Осторожно

```dataview
TABLE WITHOUT ID
  file.link as "Название",
  effect_score as "Эффект",
  main_effects as "Основные эффекты",
  side_effects as "Побочные эффекты"
FROM "09_Биохакинг/Вещества"
WHERE status = "⚠️ Осторожно"
SORT effect_score DESC
```

## 🚫 Не использовать

```dataview
TABLE WITHOUT ID
  file.link as "Название",
  effect_score as "Эффект",
  main_effects as "Основные эффекты",
  side_effects as "Побочные эффекты"
FROM "09_Биохакинг/Вещества"
WHERE status = "🚫 Не использовать"
SORT effect_score DESC
```

---

## 📊 Статистика

```dataview
TABLE WITHOUT ID
  status as "Статус",
  length(rows) as "Количество"
FROM "09_Биохакинг/Вещества"
WHERE type = "substance"
GROUP BY status
```

## 📈 Топ по эффективности

```dataview
TABLE WITHOUT ID
  file.link as "Вещество",
  effect_score as "Оценка",
  status as "Статус"
FROM "09_Биохакинг/Вещества"
WHERE effect_score != null
SORT effect_score DESC
LIMIT 5
```
