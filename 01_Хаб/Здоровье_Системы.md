# 🏥 Здоровье Системы — Мониторинг Хранилища

> Этот дашборд показывает, какие области требуют внимания, не навязчиво напоминая о важном.

---

## 📊 Общая статистика

```dataview
TABLE WITHOUT ID
  "📁 Всего заметок" as Метрика,
  length(filter(file, (f) => f.path != null)) as Значение
FROM ""
WHERE file.path != null
LIMIT 1
```

---

## 📥 Inbox — Требует обработки

```dataview
TABLE WITHOUT ID
  file.link as "Заметка",
  file.ctime as "Создана",
  round((date(today) - file.ctime) / dur(1 day)) as "Дней в Inbox"
FROM "00_Входящие"
SORT file.ctime ASC
LIMIT 10
```

> **Рекомендация:** Обрабатывайте Inbox раз в 2-3 дня. Если заметок > 10, выделите 15 минут на сортировку.

---

## 🚧 Проекты без обновлений (>7 дней)

```dataview
TABLE WITHOUT ID
  file.link as "Проект",
  file.mtime as "Последнее изменение",
  round((date(today) - file.mtime) / dur(1 day)) as "Дней без обновлений"
FROM "02_Проекты"
WHERE (date(today) - file.mtime) > dur(7 days)
SORT file.mtime ASC
```

> **Действие:** Обновите статус или переместите в Архив если проект завершён/заброшен.

---

## 🎯 Цели без прогресса (>14 дней)

```dataview
TABLE WITHOUT ID
  file.link as "Цель",
  file.mtime as "Последнее обновление",
  round((date(today) - file.mtime) / dur(1 day)) as "Дней"
FROM "01_Хаб"
WHERE contains(file.name, "Цел") AND (date(today) - file.mtime) > dur(14 days)
SORT file.mtime ASC
```

---

## 📓 Дневник — Непрерывность

```dataview
TABLE WITHOUT ID
  file.link as "День",
  mood as "Настроение",
  energy as "Энергия",
  choice(
    mood = "" OR energy = "",
    "⚠️ Неполный",
    "✅ Заполнен"
  ) as "Статус"
FROM "07_Дневник/Ежедневные"
WHERE file.day >= date(today) - dur(7 days)
SORT file.day DESC
```

### Пропущенные дни (последние 7 дней)

```dataviewjs
const dates = [];
for (let i = 0; i < 7; i++) {
    const d = moment().subtract(i, 'days');
    const path = `07_Дневник/Ежедневные/${d.format("YYYY")}/${d.format("MM")}-${d.format("MMMM")}/${d.format("DD-MM-YY")}.md`;
    const file = app.vault.getAbstractFileByPath(path);
    if (!file) {
        dates.push(d.format("DD.MM.YYYY (dddd)"));
    }
}
if (dates.length > 0) {
    dv.paragraph("**Пропущено:** " + dates.join(", "));
} else {
    dv.paragraph("✅ Все дни за неделю заполнены!");
}
```

---

## 📚 Области ответственности — Баланс

```dataview
TABLE WITHOUT ID
  file.folder as "Область",
  length(rows) as "Заметок"
FROM "03_Области"
GROUP BY file.folder
SORT length(rows) DESC
```

> **Баланс:** Если какая-то область пустая >30 дней, возможно стоит её пересмотреть.

---

## 🧠 Zettelkasten — Активность

```dataview
TABLE WITHOUT ID
  "Мимолетные" as "Тип",
  length(filter(file, (f) => f.path.startsWith("06_Цеттелькастен/Мимолетные/"))) as "Кол-во"
FROM "06_Цеттелькастен/Мимолетные"
LIMIT 1
```

```dataview
TABLE WITHOUT ID
  "Постоянные" as "Тип",
  length(filter(file, (f) => f.path.startsWith("06_Цеттелькастен/Постоянные/"))) as "Кол-во"
FROM "06_Цеттелькастен/Постоянные"
LIMIT 1
```

> **Рекомендация:** Каждую неделю превращайте минимум 1 мимолётную заметку в постоянную.

---

## 🔗 Сиротские заметки (без связей)

```dataviewjs
const orphans = [];
for (const file of app.vault.getMarkdownFiles()) {
    if (file.path.startsWith("90_Система/") || file.path.startsWith(".")) continue;

    const backlinks = app.metadataCache.getBacklinksForFile(file);
    const cache = app.metadataCache.getFileCache(file);
    const outLinks = cache?.links?.length || 0;
    const inLinks = backlinks?.data?.size || 0;

    if (outLinks === 0 && inLinks === 0) {
        orphans.push(file.path);
    }
}

if (orphans.length > 0) {
    dv.paragraph(`⚠️ **Найдено ${orphans.length} заметок без связей:**`);
    dv.list(orphans.slice(0, 10).map(p => dv.fileLink(p)));
    if (orphans.length > 10) {
        dv.paragraph(`...и ещё ${orphans.length - 10}`);
    }
} else {
    dv.paragraph("✅ Все заметки имеют связи!");
}
```

---

## ⏰ Напоминания

### Недельный обзор

```dataviewjs
const weekNum = moment().week();
const weekPath = `07_Дневник/Еженедельные/${moment().format("YYYY")}-W${String(weekNum).padStart(2, '0')}.md`;
const weekFile = app.vault.getAbstractFileByPath(weekPath);
const dayOfWeek = moment().day();

if (dayOfWeek >= 5 && !weekFile) {
    dv.paragraph("⚠️ **Пора провести недельный обзор!** Создайте заметку для этой недели.");
} else if (weekFile) {
    dv.paragraph(`✅ Недельный обзор создан: [[${weekPath}]]`);
} else {
    dv.paragraph(`📅 Недельный обзор: ещё ${7 - dayOfWeek} дней`);
}
```

### Месячный обзор

```dataviewjs
const daysInMonth = moment().daysInMonth();
const currentDay = moment().date();
const daysLeft = daysInMonth - currentDay;

if (daysLeft <= 3) {
    const monthPath = `07_Дневник/Ежемесячные/${moment().format("YYYY-MM")}.md`;
    const monthFile = app.vault.getAbstractFileByPath(monthPath);
    if (!monthFile) {
        dv.paragraph("⚠️ **Конец месяца!** Пора провести месячный обзор.");
    } else {
        dv.paragraph("✅ Месячный обзор создан.");
    }
} else {
    dv.paragraph(`📅 До конца месяца: ${daysLeft} дней`);
}
```

---

## 🎯 Рекомендации по улучшению

1. **Держите Inbox чистым** — обрабатывайте каждые 2-3 дня
2. **Обновляйте активные проекты** — минимум раз в неделю
3. **Связывайте заметки** — каждая новая заметка должна иметь 2-3 ссылки
4. **Проводите обзоры** — недельный по воскресеньям, месячный в конце месяца
5. **Развивайте Zettelkasten** — превращайте мысли в постоянные знания

---

*Последнее обновление: <% tp.date.now("DD.MM.YYYY HH:mm") %>*
