---
type: dashboard
tags: [analytics, productivity]
---

# Аналитика Продуктивности

> [!info] Автоматически агрегированные данные
> Все метрики рассчитываются на основе ваших дневниковых записей и проектов.

---

## Общая статистика

### За последний месяц (30 дней)

```dataview
TABLE WITHOUT ID
  length(rows) as "📅 Дней записано",
  round(sum(rows.mood)/length(rows), 2) as "😊 Настроение ⌀",
  round(sum(rows.energy)/length(rows), 2) as "⚡ Энергия ⌀",
  round(sum(rows.burnout_level)/length(rows), 2) as "🔥 Выгорание ⌀",
  round(length(rows) / 30 * 100) + "%" as "📈 Consistency"
FROM "07_Дневник/Ежедневные"
WHERE file.day >= date(today) - dur(30 days) AND file.day <= date(today) AND mood
GROUP BY true
```

---

## Корреляция Энергия ↔ Настроение

```dataview
TABLE WITHOUT ID
  energy as "⚡ Энергия",
  round(sum(rows.mood)/length(rows), 1) as "😊 Настроение ⌀",
  length(rows) as "Дней"
FROM "07_Дневник/Ежедневные"
WHERE mood AND energy AND file.day >= date(today) - dur(90 days)
GROUP BY energy
SORT energy ASC
```

---

## Корреляция Режим → Продуктивность

```dataview
TABLE WITHOUT ID
  mode as "📊 Режим",
  length(rows) as "Дней",
  round(sum(rows.mood)/length(rows), 1) as "😊 Настр.",
  round(sum(rows.energy)/length(rows), 1) as "⚡ Энерг.",
  round(sum(rows.burnout_level)/length(rows), 1) as "🔥 Выгор."
FROM "07_Дневник/Ежедневные"
WHERE mode AND file.day >= date(today) - dur(90 days)
GROUP BY mode
SORT length(rows) DESC
```

---

## Лучшие и худшие дни

### Топ-5 лучших дней (по энергии)

```dataview
TABLE WITHOUT ID
  file.link as "День",
  mood as "😊",
  energy as "⚡",
  burnout_level as "🔥",
  mode as "Режим"
FROM "07_Дневник/Ежедневные"
WHERE energy AND file.day >= date(today) - dur(90 days)
SORT energy DESC, mood DESC
LIMIT 5
```

### Топ-5 худших дней (по выгоранию)

```dataview
TABLE WITHOUT ID
  file.link as "День",
  mood as "😊",
  energy as "⚡",
  burnout_level as "🔥",
  mode as "Режим"
FROM "07_Дневник/Ежедневные"
WHERE burnout_level AND file.day >= date(today) - dur(90 days)
SORT burnout_level DESC, energy ASC
LIMIT 5
```

---

## Проекты — Прогресс

### Активные проекты с задачами

```dataview
TABLE WITHOUT ID
  file.link as "🚀 Проект",
  status as "Статус",
  priority as "Приоритет",
  length(file.tasks) as "Всего задач",
  length(filter(file.tasks, (t) => t.completed)) as "✅ Выполнено",
  length(filter(file.tasks, (t) => !t.completed)) as "⏳ Осталось",
  round(length(filter(file.tasks, (t) => t.completed)) / max(length(file.tasks), 1) * 100) + "%" as "Прогресс"
FROM "02_Проекты"
WHERE type = "project"
SORT choice(status = "active", 1, choice(status = "on-hold", 2, 3)) ASC, priority ASC
```

### Просроченные проекты

```dataview
TABLE WITHOUT ID
  file.link as "⚠️ Проект",
  due as "Дедлайн",
  round((date(today) - date(due)).days) + " дней" as "Просрочено"
FROM "02_Проекты"
WHERE type = "project" AND status = "active" AND due AND date(due) < date(today)
SORT due ASC
```

---

## Задачи — Анализ

### Незавершенные задачи по приоритету

```dataview
TASK
FROM "02_Проекты" OR "01_Хаб"
WHERE !completed
GROUP BY choice(contains(text, "🔴") OR contains(text, "[A]"), "🔴 A-Приоритет",
  choice(contains(text, "🟡") OR contains(text, "[B]"), "🟡 B-Приоритет", "🟢 C-Приоритет"))
LIMIT 20
```

---

## Триггеры и Поддержка

### Частые триггеры стресса

```dataview
LIST WITHOUT ID
  "⚠️ " + trigger
FROM "07_Дневник/Ежедневные"
WHERE trigger AND file.day >= date(today) - dur(30 days)
SORT file.day DESC
LIMIT 10
```

### Что помогает

```dataview
LIST WITHOUT ID
  "✅ " + support
FROM "07_Дневник/Ежедневные"
WHERE support AND file.day >= date(today) - dur(30 days)
SORT file.day DESC
LIMIT 10
```

---

## Паттерны по дням недели

```dataview
TABLE WITHOUT ID
  dateformat(file.day, "EEEE") as "День недели",
  round(sum(rows.mood)/length(rows), 1) as "😊 Настр. ⌀",
  round(sum(rows.energy)/length(rows), 1) as "⚡ Энерг. ⌀",
  length(rows) as "Записей"
FROM "07_Дневник/Ежедневные"
WHERE mood AND file.day >= date(today) - dur(90 days)
GROUP BY dateformat(file.day, "EEEE")
SORT choice(dateformat(file.day, "EEEE") = "Monday", 1,
  choice(dateformat(file.day, "EEEE") = "Tuesday", 2,
  choice(dateformat(file.day, "EEEE") = "Wednesday", 3,
  choice(dateformat(file.day, "EEEE") = "Thursday", 4,
  choice(dateformat(file.day, "EEEE") = "Friday", 5,
  choice(dateformat(file.day, "EEEE") = "Saturday", 6, 7)))))) ASC
```

---

## Insights

> [!tip] Рекомендации на основе данных
> - Отслеживайте дни с высокой энергией — что вы делали накануне?
> - Анализируйте триггеры стресса и избегайте их
> - Используйте паттерны дней недели для планирования сложных задач

---

## Навигация

← [[Главная]] | [[Цели_2025]] | [[Стоп_Лист]]
