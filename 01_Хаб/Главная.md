# Home — Командный центр

## Сегодня

📅 **Дата:** <% tp.date.now("DD MMMM YYYY, dddd", "ru") %>

### Быстрые действия

- 🌅 **Создать дневник Lite:** Используйте команду `Daily_Lite` (Ctrl/Cmd+P → "Daily_Lite")
- 📝 **Создать дневник Full:** Используйте команду `Daily_Full` (Ctrl/Cmd+P → "Daily_Full")

---

## Текущий день

```dataview
TABLE WITHOUT ID
  mode as "Режим",
  mood as "Настроение",
  energy as "Энергия",
  burnout_level as "Выгорание"
FROM "07_Дневник/Ежедневные"
WHERE file.day = date(today)
LIMIT 1
```

## Фокус недели

📌 Ссылка на текущий недельный обзор: [[07_Дневник/Еженедельные]]

## Биохакинг (сводка)

```dataview
TABLE WITHOUT ID
  status as "Статус",
  length(rows) as "Количество"
FROM "09_Биохакинг/Вещества"
GROUP BY status
SORT status ASC
```

## Работа (часы за последний период)

```dataview
TABLE WITHOUT ID
  file.link as "День",
  length(filter(file.lists, (l) => contains(l.text, "#clockify"))) as "Записей"
FROM "07_Дневник/Ежедневные"
WHERE file.day >= date(today) - dur(7 days) AND file.day <= date(today)
SORT file.day DESC
```

> **Примечание:** Для детального учета времени откройте дневниковые заметки и просмотрите секцию "Рабочий лог".

