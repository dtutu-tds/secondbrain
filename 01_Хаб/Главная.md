# 🧠 Командный Центр

## Сегодня

📅 **Дата:** `= date(today)`

---

## Быстрые действия

> [!tip] QuickAdd команды (Ctrl/Cmd+P)
> - `Daily_Lite` — Быстрый дневник (5-10 мин)
> - `Daily_Full` — Полный дневник (15-20 мин)
> - `Work_Block` — Залогировать работу
> - `New_Task` — Добавить задачу
> - `Bio_Stack` — Записать вещество

---

## Текущее состояние

```dataview
TABLE WITHOUT ID
  "📊 " + mode as "Режим",
  "😊 " + mood + "/5" as "Настроение",
  "⚡ " + energy + "/5" as "Энергия",
  "🔥 " + burnout_level + "/5" as "Выгорание",
  "🎯 " + choice(length(file.tasks.completed) > 0,
    round(length(file.tasks.completed) / length(file.tasks) * 100) + "%",
    "—") as "Задачи"
FROM "07_Дневник/Ежедневные"
WHERE file.day = date(today)
LIMIT 1
```

---

## 🚨 Требует внимания

```dataviewjs
// Проактивный мониторинг системы
const monthMap = {
    "01": "01-Январь", "02": "02-Февраль", "03": "03-Март",
    "04": "04-Апрель", "05": "05-Май", "06": "06-Июнь",
    "07": "07-Июль", "08": "08-Август", "09": "09-Сентябрь",
    "10": "10-Октябрь", "11": "11-Ноябрь", "12": "12-Декабрь"
};

let alerts = [];

try {
    // Inbox переполнен
    const inboxFiles = app.vault.getMarkdownFiles().filter(f => f.path.startsWith("00_Входящие/"));
    if (inboxFiles.length > 5) {
        alerts.push(`📥 **Inbox:** ${inboxFiles.length} заметок требуют обработки`);
    }

    // Застоявшиеся проекты
    const projectFiles = app.vault.getMarkdownFiles().filter(f => f.path.startsWith("02_Проекты/"));
    let staleProjects = 0;
    for (const pf of projectFiles) {
        const daysSince = (Date.now() - pf.stat.mtime) / (1000 * 60 * 60 * 24);
        if (daysSince > 7) staleProjects++;
    }
    if (staleProjects > 0) {
        alerts.push(`🔄 **Проекты:** ${staleProjects} без обновлений >7 дней`);
    }

    // Недельный обзор
    const weekNum = moment().week();
    const weekPath = `07_Дневник/Еженедельные/${moment().format("YYYY")}-W${String(weekNum).padStart(2, '0')}.md`;
    const weekFile = app.vault.getAbstractFileByPath(weekPath);
    const dayOfWeek = moment().day();
    if (dayOfWeek >= 5 && !weekFile) {
        alerts.push("📊 **Недельный обзор** — пора провести!");
    }

    // Пропущенные дневники
    let missedDays = 0;
    for (let i = 1; i <= 3; i++) {
        const d = moment().subtract(i, 'days');
        const monthFolder = monthMap[d.format("MM")];
        const path = `07_Дневник/Ежедневные/${d.format("YYYY")}/${monthFolder}/${d.format("DD-MM-YY")}.md`;
        if (!app.vault.getAbstractFileByPath(path)) missedDays++;
    }
    if (missedDays > 0) {
        alerts.push(`📓 **Дневник:** пропущено ${missedDays} дней за последние 3 дня`);
    }

    if (alerts.length > 0) {
        dv.list(alerts);
    } else {
        dv.paragraph("✅ Всё в порядке! Система работает отлично.");
    }
} catch (e) {
    dv.paragraph("ℹ️ Проверка алертов недоступна: " + e.message);
}
```

---

## Статистика за 7 дней

### Средние показатели

```dataview
TABLE WITHOUT ID
  round(sum(rows.mood)/length(rows), 1) as "Настр. ⌀",
  round(sum(rows.energy)/length(rows), 1) as "Энерг. ⌀",
  round(sum(rows.burnout_level)/length(rows), 1) as "Выгор. ⌀",
  length(rows) as "Дней записано"
FROM "07_Дневник/Ежедневные"
WHERE file.day >= date(today) - dur(7 days) AND file.day <= date(today)
GROUP BY true
```

### Тренд настроения

```dataview
LIST WITHOUT ID
  "📅 " + file.day.day + "." + file.day.month + " → 😊 " + mood + " | ⚡ " + energy + " | 🔥 " + burnout_level
FROM "07_Дневник/Ежедневные"
WHERE file.day >= date(today) - dur(7 days) AND mood
SORT file.day ASC
```

---

## Активные проекты

```dataview
TABLE WITHOUT ID
  "🚀 " + file.link as "Проект",
  "📋 " + choice(priority = "A", "🔴", choice(priority = "B", "🟡", "🟢")) + " " + priority as "Приоритет",
  "⏰ " + choice(due, due, "—") as "Дедлайн",
  "📊 " + round(length(filter(file.tasks, (t) => t.completed)) / max(length(file.tasks), 1) * 100) + "%" as "Прогресс"
FROM "02_Проекты"
WHERE type = "project" AND status = "active"
SORT priority ASC, due ASC
LIMIT 5
```

### Срочные задачи из проектов (A/B приоритет)

```dataview
TASK
FROM "02_Проекты"
WHERE !completed AND (contains(text, "🔴") OR contains(text, "[A]") OR contains(text, "[B]"))
SORT file.name ASC
LIMIT 10
```

---

## Цели 2025 — Прогресс

```dataview
TABLE WITHOUT ID
  file.link as "Цель",
  area as "Область",
  priority as "Приоритет",
  deadline as "Срок"
FROM "01_Хаб" OR "02_Проекты"
WHERE type = "goal"
SORT priority ASC
```

---

## Продуктивность

### Рабочее время (последние 7 дней)

```dataview
TABLE WITHOUT ID
  dateformat(file.day, "dd.MM") as "День",
  length(filter(file.lists, (l) => contains(l.text, "#clockify"))) as "Записей",
  choice(distraction_free, distraction_free, "—") as "Фокус-время"
FROM "07_Дневник/Ежедневные"
WHERE file.day >= date(today) - dur(7 days) AND file.day <= date(today)
SORT file.day DESC
```

### Streak (серия дней)

```dataview
LIST WITHOUT ID
  "🔥 Дней подряд: " + length(rows)
FROM "07_Дневник/Ежедневные"
WHERE file.day >= date(today) - dur(30 days) AND file.day <= date(today)
GROUP BY true
```

---

## Биохакинг — Аналитика

### По статусу

```dataview
TABLE WITHOUT ID
  status as "Статус",
  length(rows) as "Кол-во",
  round(sum(rows.effect_score)/length(rows), 1) as "⌀ Эффект"
FROM "09_Биохакинг/Вещества"
WHERE type = "substance"
GROUP BY status
SORT choice(status = "✅ Использовать", 1, choice(status = "⚠️ Осторожно", 2, 3)) ASC
```

### Топ-3 эффективных вещества

```dataview
TABLE WITHOUT ID
  file.link as "Вещество",
  effect_score as "Оценка",
  main_effects as "Эффекты"
FROM "09_Биохакинг/Вещества"
WHERE type = "substance" AND status = "✅ Использовать"
SORT effect_score DESC
LIMIT 3
```

### Последние записи о биохакинге

```dataview
LIST WITHOUT ID
  "📅 " + dateformat(file.day, "dd.MM") + " → " + bio_substances + ": " + bio_details
FROM "07_Дневник/Ежедневные"
WHERE bio_substances AND file.day >= date(today) - dur(14 days)
SORT file.day DESC
LIMIT 5
```

---

## Здоровье и Психика

### Последние записи

```dataview
TABLE WITHOUT ID
  dateformat(file.day, "dd.MM") as "Дата",
  choice(health, health, "—") as "Здоровье",
  choice(psych, psych, "—") as "Психика"
FROM "07_Дневник/Ежедневные"
WHERE (health OR psych) AND file.day >= date(today) - dur(7 days)
SORT file.day DESC
LIMIT 7
```

---

## Финансы — Последние записи

```dataview
LIST WITHOUT ID
  "💰 " + dateformat(file.day, "dd.MM") + " → " + finance
FROM "07_Дневник/Ежедневные"
WHERE finance AND file.day >= date(today) - dur(30 days)
SORT file.day DESC
LIMIT 10
```

---

## Zettelkasten — Активность

```dataview
TABLE WITHOUT ID
  "Мимолетные" as "Тип",
  length(filter(file.lists, (f) => true)) as "Заметок"
FROM "06_Цеттелькастен/Мимолетные"
GROUP BY true
```

```dataview
TABLE WITHOUT ID
  "Постоянные" as "Тип",
  length(rows) as "Заметок"
FROM "06_Цеттелькастен/Постоянные"
GROUP BY true
```

### Последние идеи

```dataview
LIST
FROM "06_Цеттелькастен/Мимолетные" OR "06_Цеттелькастен/Постоянные"
SORT file.mtime DESC
LIMIT 5
```

---

## Навигация

- 🎯 [[Цели_2025]] — Годовые цели
- 🚫 [[Стоп_Лист]] — Что НЕ делать
- 📝 [[Списки/Мастер_Список]] — Разгрузка мозга
- 🗂️ [[80_Канбан/Доска_Жизни]] — Визуальное управление
- 📊 [[09_Биохакинг/Био_Дашборд]] — Биохакинг центр
- 📈 [[01_Хаб/Аналитика]] — Расширенная аналитика
- 🏥 [[01_Хаб/Здоровье_Системы]] — Здоровье системы

---

> [!info] Автоматическое обновление
> Все данные на этой странице обновляются автоматически через Dataview при каждом открытии.
