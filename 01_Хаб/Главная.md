# 🧠 Командный Центр

<%*
// Умное приветствие и контекст
moment.locale('ru');
const hour = moment().hour();
let greeting = "";
let emoji = "";
let contextQuestion = "";

if (hour >= 5 && hour < 12) {
    greeting = "Доброе утро";
    emoji = "🌅";
    contextQuestion = "Какая ОДНА задача сделает этот день успешным?";
} else if (hour >= 12 && hour < 18) {
    greeting = "Добрый день";
    emoji = "☀️";
    contextQuestion = "Что уже сделано? Какой прогресс?";
} else if (hour >= 18 && hour < 23) {
    greeting = "Добрый вечер";
    emoji = "🌙";
    contextQuestion = "Что дало 80% результата сегодня?";
} else {
    greeting = "Доброй ночи";
    emoji = "🌃";
    contextQuestion = "Завтра начнётся с чего?";
}

// Маппинг месяцев для путей
const monthMap = {
    "01": "01-Январь", "02": "02-Февраль", "03": "03-Март",
    "04": "04-Апрель", "05": "05-Май", "06": "06-Июнь",
    "07": "07-Июль", "08": "08-Август", "09": "09-Сентябрь",
    "10": "10-Октябрь", "11": "11-Ноябрь", "12": "12-Декабрь"
};
const currentMonth = monthMap[moment().format("MM")];

tR += `${emoji} **${greeting}!** | 📅 ${moment().format("DD MMMM YYYY, dddd")}\n`;
_%>

---

## ⚡ Быстрые действия

| Действие | Команда |
|----------|---------|
| 📝 Lite дневник | `Ctrl+P` → `Daily_Lite` |
| 📓 Full дневник | `Ctrl+P` → `Daily_Full` |
| 💡 Быстрая идея | `Ctrl+P` → `Quick_Idea` |
| ✅ Новая задача | `Ctrl+P` → `New_Task` |
| 🧬 Био-запись | `Ctrl+P` → `Bio_Stack` |

---

## 📊 Состояние сегодня

```dataviewjs
// Маппинг месяцев
const monthMap = {
    "01": "01-Январь", "02": "02-Февраль", "03": "03-Март",
    "04": "04-Апрель", "05": "05-Май", "06": "06-Июнь",
    "07": "07-Июль", "08": "08-Август", "09": "09-Сентябрь",
    "10": "10-Октябрь", "11": "11-Ноябрь", "12": "12-Декабрь"
};
const currentMonth = monthMap[moment().format("MM")];
const todayPath = `07_Дневник/Ежедневные/${moment().format("YYYY")}/${currentMonth}/${moment().format("DD-MM-YY")}.md`;

try {
    const file = app.vault.getAbstractFileByPath(todayPath);
    if (file) {
        const page = dv.page(todayPath);
        if (page) {
            dv.table(["Параметр", "Значение"], [
                ["😊 Настроение", page.mood || "—"],
                ["⚡ Энергия", page.energy || "—"],
                ["🔥 Выгорание", page.burnout_level || "—"],
                ["🎯 Режим", page.mode || "—"]
            ]);
            dv.paragraph(`[[${todayPath}|📓 Открыть дневник]]`);
        }
    } else {
        dv.paragraph("⚠️ **Дневник на сегодня не создан**");
        dv.paragraph("Используйте Quick Action выше для создания");
    }
} catch (e) {
    dv.paragraph("❌ Ошибка загрузки дневника: " + e.message);
}
```

---

## 🎯 Контекстный вопрос

<%*
tR += `> **${contextQuestion}**`;
_%>

---

## 🚨 Требует внимания

```dataviewjs
// Маппинг месяцев для корректных путей
const monthMap = {
    "01": "01-Январь", "02": "02-Февраль", "03": "03-Март",
    "04": "04-Апрель", "05": "05-Май", "06": "06-Июнь",
    "07": "07-Июль", "08": "08-Август", "09": "09-Сентябрь",
    "10": "10-Октябрь", "11": "11-Ноябрь", "12": "12-Декабрь"
};

let alerts = [];

try {
    // Inbox
    const inboxFiles = app.vault.getMarkdownFiles().filter(f => f.path.startsWith("00_Входящие/"));
    if (inboxFiles.length > 5) {
        alerts.push(`📥 **Inbox:** ${inboxFiles.length} заметок требуют обработки`);
    }

    // Проекты без обновлений
    const projectFiles = app.vault.getMarkdownFiles().filter(f => f.path.startsWith("02_Проекты/"));
    let staleProjects = 0;
    for (const pf of projectFiles) {
        const daysSince = (Date.now() - pf.stat.mtime) / (1000 * 60 * 60 * 24);
        if (daysSince > 7) staleProjects++;
    }
    if (staleProjects > 0) {
        alerts.push(`🔄 **Проекты:** ${staleProjects} без обновлений >7 дней`);
    }

    // Недельный обзор
    const weekNum = moment().week();
    const weekPath = `07_Дневник/Еженедельные/${moment().format("YYYY")}-W${String(weekNum).padStart(2, '0')}.md`;
    const weekFile = app.vault.getAbstractFileByPath(weekPath);
    const dayOfWeek = moment().day();
    if (dayOfWeek >= 5 && !weekFile) {
        alerts.push("📊 **Недельный обзор** — пора провести!");
    }

    // Пропущенные дни (с правильным маппингом месяцев)
    let missedDays = 0;
    for (let i = 1; i <= 3; i++) {
        const d = moment().subtract(i, 'days');
        const monthFolder = monthMap[d.format("MM")];
        const path = `07_Дневник/Ежедневные/${d.format("YYYY")}/${monthFolder}/${d.format("DD-MM-YY")}.md`;
        if (!app.vault.getAbstractFileByPath(path)) missedDays++;
    }
    if (missedDays > 0) {
        alerts.push(`📓 **Дневник:** пропущено ${missedDays} дней за последние 3 дня`);
    }

    if (alerts.length > 0) {
        dv.list(alerts);
    } else {
        dv.paragraph("✅ Всё в порядке! Система работает отлично.");
    }
} catch (e) {
    dv.paragraph("❌ Ошибка проверки: " + e.message);
}
```

---

## 📈 Тренды (7 дней)

```dataview
TABLE WITHOUT ID
  file.link as "День",
  mood as "😊",
  energy as "⚡",
  burnout_level as "🔥"
FROM "07_Дневник/Ежедневные"
WHERE file.day >= date(today) - dur(7 days) AND file.day <= date(today)
SORT file.day DESC
```

---

## 🎯 Активные проекты

```dataview
TABLE WITHOUT ID
  file.link as "Проект",
  status as "Статус",
  priority as "Приоритет",
  round((date(today) - file.mtime) / dur(1 day)) as "Дней с обновления"
FROM "02_Проекты"
WHERE status != "completed" AND status != "archived"
SORT priority ASC
LIMIT 5
```

---

## 🔗 Быстрая навигация

- [[01_Хаб/Аналитика|📈 Аналитика и тренды]]
- [[01_Хаб/Здоровье_Системы|🏥 Здоровье системы]]
- [[01_Хаб/Цели_2025|🎯 Цели 2025]]
- [[01_Хаб/Стоп_Лист|🛑 Стоп-лист]]
- [[80_Канбан/Доска_Жизни|📋 Канбан доска]]
- [[09_Биохакинг/Био_Дашборд|🧬 Биохакинг]]
- [[07_Дневник/Еженедельные|📊 Недельные обзоры]]

---

*Последнее обновление: автоматически при каждом открытии*
