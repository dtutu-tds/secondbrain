# 🎯 Прогресс по целям 2025

> Визуальный трекинг прогресса с автоматическими метриками

---

## 📊 Общий прогресс

```dataviewjs
const goals = dv.pages('"01_Хаб" OR "01_Хаб/Цели"')
    .where(p => p.type === "goal" || p.file.name.includes("Цел"));

dv.paragraph(`**Всего целей:** ${goals.length}`);

// Подсчёт по областям
const areas = {};
for (const g of goals) {
    const area = g.area || "Общее";
    if (!areas[area]) areas[area] = 0;
    areas[area]++;
}

dv.table(["Область", "Количество целей"],
    Object.entries(areas).map(([k, v]) => [k, v]));
```

---

## 🎯 Цели по областям

### 💼 Работа и карьера

```dataview
TABLE WITHOUT ID
  file.link as "Цель",
  priority as "Приоритет",
  deadline as "Дедлайн",
  round((date(today) - file.mtime) / dur(1 day)) as "Дней без обновления"
FROM "01_Хаб"
WHERE (type = "goal" AND area = "work") OR contains(file.name, "Работа")
SORT priority ASC
```

### 💰 Финансы

```dataview
TABLE WITHOUT ID
  file.link as "Цель",
  priority as "Приоритет",
  deadline as "Дедлайн"
FROM "01_Хаб"
WHERE (type = "goal" AND area = "finance") OR contains(file.name, "Финанс")
SORT priority ASC
```

### 🏃 Здоровье

```dataview
TABLE WITHOUT ID
  file.link as "Цель",
  priority as "Приоритет",
  deadline as "Дедлайн"
FROM "01_Хаб"
WHERE (type = "goal" AND area = "health") OR contains(file.name, "Здоров")
SORT priority ASC
```

### 📚 Обучение

```dataview
TABLE WITHOUT ID
  file.link as "Цель",
  priority as "Приоритет",
  deadline as "Дедлайн"
FROM "01_Хаб"
WHERE (type = "goal" AND area = "learning") OR contains(file.name, "Обучен")
SORT priority ASC
```

### 👥 Отношения

```dataview
TABLE WITHOUT ID
  file.link as "Цель",
  priority as "Приоритет",
  deadline as "Дедлайн"
FROM "01_Хаб"
WHERE (type = "goal" AND area = "relationships") OR contains(file.name, "Отношен")
SORT priority ASC
```

### 🎨 Творчество

```dataview
TABLE WITHOUT ID
  file.link as "Цель",
  priority as "Приоритет",
  deadline as "Дедлайн"
FROM "01_Хаб"
WHERE (type = "goal" AND area = "creativity") OR contains(file.name, "Творчест")
SORT priority ASC
```

---

## 📅 Временная шкала

### Ближайшие дедлайны (30 дней)

```dataview
TABLE WITHOUT ID
  file.link as "Цель",
  deadline as "Дедлайн",
  (deadline - date(today)) as "Осталось"
FROM "01_Хаб"
WHERE type = "goal" AND deadline != null AND deadline <= date(today) + dur(30 days)
SORT deadline ASC
```

### Просроченные

```dataview
LIST
FROM "01_Хаб"
WHERE type = "goal" AND deadline != null AND deadline < date(today)
```

---

## 🔄 Связь с проектами

```dataviewjs
const goals = dv.pages('"01_Хаб"').where(p => p.type === "goal");
const projects = dv.pages('"02_Проекты"');

let orphanGoals = [];

for (const g of goals) {
    let hasProject = false;
    for (const p of projects) {
        // Проверяем связи
        const content = await dv.io.load(p.file.path);
        if (content.includes(g.file.name)) {
            hasProject = true;
            break;
        }
    }
    if (!hasProject) {
        orphanGoals.push(g.file.link);
    }
}

if (orphanGoals.length > 0) {
    dv.paragraph("⚠️ **Цели без проектов:**");
    dv.list(orphanGoals);
} else {
    dv.paragraph("✅ Все цели имеют связанные проекты");
}
```

---

## 📊 Рекомендации

```dataviewjs
let recommendations = [];

// Проверяем цели без обновлений
const staleGoals = dv.pages('"01_Хаб"')
    .where(p => p.type === "goal" && (dv.date("today") - p.file.mtime) > dv.duration("14 days"));

if (staleGoals.length > 0) {
    recommendations.push(`📝 ${staleGoals.length} целей не обновлялись >14 дней`);
}

// Проверяем баланс областей
const areas = {};
for (const g of dv.pages('"01_Хаб"').where(p => p.type === "goal")) {
    const area = g.area || "other";
    if (!areas[area]) areas[area] = 0;
    areas[area]++;
}

const areaCount = Object.keys(areas).length;
if (areaCount < 4) {
    recommendations.push("🎯 Рассмотрите цели в других областях жизни");
}

if (recommendations.length > 0) {
    dv.list(recommendations);
} else {
    dv.paragraph("✅ Цели сбалансированы и актуальны");
}
```

---

## 🎯 Быстрые действия

- [[90_Система/Шаблоны/Goal|➕ Создать новую цель]]
- [[01_Хаб/Цели_2025|📋 Все цели 2025]]
- [[01_Хаб/Аналитика|📈 Аналитика]]
- [[07_Дневник/Ежеквартальные|📊 Квартальные обзоры]]

---

*Обновляется автоматически*
