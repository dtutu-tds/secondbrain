# Дополнительные плагины для комплексной системы

> [!important] Установка плагинов
> Settings → Community Plugins → Browse → Поиск → Install → Enable

---

## 1. DB Folder (Database Folder)

**Что это:** Превращает папку в базу данных с табличным интерфейсом, подобным Notion.

**Установка:**
1. Community Plugins → Browse → "DB Folder"
2. Install → Enable

**Как использовать:**

В папке `02_Проекты` создайте файл `.dbfolder`:

```yaml
---
database: projects
description: Database for project management
fields:
  - name: status
    type: select
    options:
      - active
      - on-hold
      - completed
  - name: priority
    type: select
    options:
      - A
      - B
      - C
  - name: area
    type: text
  - name: due
    type: date
  - name: start
    type: date
---
```

**Результат:** Папка откроется как интерактивная таблица с фильтрацией и сортировкой.

---

## 2. Obsidian Charts

**Что это:** Визуализация данных графиками (линейные, круговые, столбчатые).

**Установка:**
1. Community Plugins → Browse → "Obsidian Charts"
2. Install → Enable

**Примеры использования:**

### График настроения за неделю

````markdown
```chart
type: line
labels: [Пн, Вт, Ср, Чт, Пт, Сб, Вс]
series:
  - title: Настроение
    data: [3, 4, 4, 3, 5, 4, 3]
  - title: Энергия
    data: [2, 3, 4, 4, 4, 3, 2]
width: 100%
beginAtZero: true
```
````

### Распределение приоритетов проектов (Pie Chart)

````markdown
```chart
type: pie
labels: [A-Приоритет, B-Приоритет, C-Приоритет]
series:
  - title: Проекты
    data: [3, 5, 2]
width: 50%
```
````

### Динамический график (с Dataview)

````markdown
```dataviewjs
const pages = dv.pages('"07_Дневник/Ежедневные"')
  .where(p => p.mood && p.file.day >= dv.date('today').minus({days: 7}))
  .sort(p => p.file.day);

const labels = pages.map(p => p.file.day.toFormat("dd.MM")).array();
const moodData = pages.map(p => p.mood).array();
const energyData = pages.map(p => p.energy).array();

const chartData = {
  type: 'line',
  data: {
    labels: labels,
    datasets: [
      {
        label: 'Настроение',
        data: moodData,
        borderColor: '#4A90E2'
      },
      {
        label: 'Энергия',
        data: energyData,
        borderColor: '#7ED321'
      }
    ]
  }
};

window.renderChart(chartData, this.container);
```
````

---

## 3. Buttons

**Что это:** Создание кнопок для быстрых действий (запуск команд, создание заметок, выполнение скриптов).

**Установка:**
1. Community Plugins → Browse → "Buttons"
2. Install → Enable

**Примеры кнопок:**

### Создать дневник

````markdown
```button
name Create Daily Lite
type command
action QuickAdd: Daily_Lite
color blue
```
````

### Открыть главную страницу

````markdown
```button
name Go to Home
type link
action [[01_Хаб/Главная]]
color green
```
````

### Записать работу

````markdown
```button
name Log Work
type command
action QuickAdd: Work_Block
color purple
```
````

### Inline кнопки (в строку)

```markdown
`button-dailylite` `button-worklog` `button-newtask`
```

**Где определить кнопки глобально:**
В файле `90_Система/Кнопки.md`:

````markdown
```button
name dailylite
type command
action QuickAdd: Daily_Lite
```

```button
name worklog
type command
action QuickAdd: Work_Block
```

```button
name newtask
type command
action QuickAdd: New_Task
```
````

---

## 4. Metadata Menu

**Что это:** Быстрое редактирование YAML frontmatter через меню (dropdown, autocomplete).

**Установка:**
1. Community Plugins → Browse → "Metadata Menu"
2. Install → Enable

**Настройка полей:**

Settings → Metadata Menu → Manage Fields:

```yaml
# status field
name: status
type: Select
options:
  - active
  - on-hold
  - completed

# priority field
name: priority
type: Select
options:
  - A
  - B
  - C

# mood field
name: mood
type: Number
minValue: 1
maxValue: 5

# energy field
name: energy
type: Number
minValue: 1
maxValue: 5

# burnout_level field
name: burnout_level
type: Number
minValue: 1
maxValue: 5

# mode field
name: mode
type: Select
options:
  - work
  - study
  - mixed
  - rest

# type field
name: type
type: Select
options:
  - daily
  - weekly
  - monthly
  - project
  - goal
  - substance
  - area

# effect_score field
name: effect_score
type: Number
minValue: -5
maxValue: 5
```

**Результат:** При клике на свойство в frontmatter откроется удобное меню для быстрого выбора.

---

## 5. Auto Link Title

**Что это:** Автоматически вставляет заголовок страницы при вставке ссылки.

**Установка:**
1. Community Plugins → Browse → "Auto Link Title"
2. Install → Enable

**Результат:** Вставляете URL → автоматически получаете `[Заголовок страницы](URL)`

---

## 6. Natural Language Dates

**Что это:** Ввод дат на естественном языке ("next monday", "tomorrow", "in 3 days").

**Установка:**
1. Community Plugins → Browse → "Natural Language Dates"
2. Install → Enable

**Использование:**
- Typing `@tomorrow` → автоматически превращается в дату
- `@next friday` → дата следующей пятницы
- `@in 2 weeks` → дата через 2 недели

---

## 7. Dataview JS (расширенный Dataview)

Уже установлен! Для использования JavaScript в запросах:

````markdown
```dataviewjs
// Пример: подсчет общего времени работы
const days = dv.pages('"07_Дневник/Ежедневные"')
  .where(p => p.file.day >= dv.date('today').minus({days: 7}));

let totalHours = 0;
for (let day of days) {
  // Парсим записи с #clockify
  const lists = day.file.lists.where(l => l.text.includes("#clockify"));
  for (let item of lists) {
    const match = item.text.match(/~(\d+\.?\d*)/);
    if (match) totalHours += parseFloat(match[1]);
  }
}

dv.paragraph(`**Всего часов за 7 дней:** ${totalHours.toFixed(1)}h`);
```
````

---

## 8. Templater (уже установлен)

### Полезные команды Templater

```markdown
<%*
// Текущая дата
const date = tp.date.now("YYYY-MM-DD");

// Предыдущий день
const yesterday = tp.date.now("YYYY-MM-DD", -1);

// Название файла без расширения
const filename = tp.file.title;

// Перемещение файла
await tp.file.move("02_Проекты/" + filename);

// Создание файла
await tp.file.create_new("Контент", "Имя файла", false, "02_Проекты");

// Ввод от пользователя
const project = await tp.system.prompt("Название проекта:");

// Выбор из списка
const priority = await tp.system.suggester(
  ["🔴 A - Критично", "🟡 B - Важно", "🟢 C - Можно отложить"],
  ["A", "B", "C"]
);
%>
```

---

## Рекомендуемый порядок установки

1. **Metadata Menu** — быстрое редактирование свойств
2. **Buttons** — кнопки для частых действий
3. **Obsidian Charts** — визуализация прогресса
4. **Natural Language Dates** — удобный ввод дат
5. **DB Folder** — табличное представление папок
6. **Auto Link Title** — автоматические заголовки ссылок

---

## Проверка установки

После установки плагинов:
1. Settings → Community Plugins → убедитесь что все включены
2. Перезапустите Obsidian (Ctrl/Cmd+R)
3. Протестируйте функции на главной странице

---

← [[90_Система/Руководство_Системы|Руководство системы]]
