---
type: quarterly_review
quarter: Q<% Math.ceil((tp.date.now("M")) / 3) %>
year: <% tp.date.now("YYYY") %>
created: <% tp.date.now("YYYY-MM-DD") %>
tags:
  - review
  - quarterly
---

# 🎯 Квартальный обзор — Q<% Math.ceil((tp.date.now("M")) / 3) %> <% tp.date.now("YYYY") %>

---

## 📊 Обзор квартала

### Главные победы (ТОП-5)

1.
2.
3.
4.
5.

### Главные уроки

1.
2.
3.

### Что не удалось и почему

1.
2.
3.

---

## 🎯 Прогресс по годовым целям

| Цель | Прогресс | Статус | Корректировка |
|------|----------|--------|---------------|
|  | % |  |  |

---

## 📈 Ключевые метрики квартала

### Продуктивность

```dataviewjs
const quarterStart = moment().startOf('quarter');
const quarterEnd = moment().endOf('quarter');

const pages = dv.pages('"07_Дневник/Ежедневные"')
    .where(p => p.file.day >= dv.date(quarterStart.format("YYYY-MM-DD")) &&
                p.file.day <= dv.date(quarterEnd.format("YYYY-MM-DD")));

dv.paragraph(`**Дней в дневнике:** ${pages.length}`);

let avgEnergy = 0, count = 0;
for (const p of pages) {
    if (p.energy) { avgEnergy += p.energy; count++; }
}
if (count > 0) {
    dv.paragraph(`**Средняя энергия:** ${(avgEnergy/count).toFixed(1)}/10`);
}
```

### Проекты

```dataview
TABLE WITHOUT ID
  "Завершено проектов" as Метрика,
  length(rows) as Значение
FROM "05_Архив"
WHERE file.mtime >= date(sow) - dur(90 days)
GROUP BY true
```

---

## 🔄 90-дневный цикл

### Что работало (продолжать)

-

### Что требует корректировки

-

### Новые эксперименты

-

---

## 🎯 Цели на следующий квартал

### Главная цель Q<% (Math.ceil((tp.date.now("M")) / 3) % 4) + 1 %>



### Ключевые результаты (KRs)

1.
2.
3.

### Проекты для запуска

1.
2.
3.

---

## 💡 Стратегические инсайты



---

*Дата создания: <% tp.date.now("DD.MM.YYYY") %>*
